# =============================================================
# Tenet Clinical AI - all dependencies.
# Install with:  pip install -r requirements.txt
# =============================================================

# ---- Web API ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# ---- Data ----
numpy>=1.26
pandas>=2.2

# ---- Embeddings, transformers, ML ----
sentence-transformers>=2.6      # embeddings + cross-encoder reranker
torch>=2.2                      # runs the models
transformers>=4.40              # tokenizers / classification models
scikit-learn>=1.4               # classifier, clustering, metrics
matplotlib>=3.8                 # graphs (notebooks)

# ---- RAG: chunking, keyword search ----
langchain-text-splitters>=0.0.1 # recursive chunking
rank-bm25>=0.2                   # BM25 keyword search

# ---- Vector database ----
pinecone>=5.0                    # managed cloud vector DB

# ---- LLM ----
anthropic>=0.25                  # Claude (used when APP_MODE=online)

# ---- Agent + observability ----
langgraph>=0.2                   # the tool-choosing agent
langsmith>=0.1                   # optional tracing (enable via env vars)

# ---- UI ----
streamlit>=1.30                  # the chat web page

# ---- MLOps ----
mlflow>=2.11                     # experiment tracking

# ---- Testing ----
pytest>=8.1
httpx>=0.27

# ---- Fine-tuning (used in the Colab notebook, not required locally) ----
# peft>=0.10
# datasets>=2.18
# accelerate>=0.29

# ---- Notebooks (optional, for running the lesson .ipynb files) ----
# ipykernel>=6.29
